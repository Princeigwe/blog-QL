type Author{
  name: String
  age: Int
}

input AuthorInput{
  name: String!
  age: Int!
}

type AuthorResponse{
  message: String
  author: Author
}

type Mutation{
  createAuthor(input: AuthorInput): AuthorResponse
}